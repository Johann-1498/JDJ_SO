package Memoria;

public class MMU {
    // Clase auxiliar para traducir direcciones lógicas a físicas
    // En esta simulación de alto nivel, actúa más como validador.
    
    public static boolean validarAcceso(int pid, MemoriaFisica memoria) {
        // Lógica futura: verificar si el proceso intenta acceder memoria prohibida
        return true;
    }
}